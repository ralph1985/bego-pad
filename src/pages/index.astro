---
import "../firebase";
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bego-Pad</title>
  </head>
  <body>
    <main style="max-width:720px;margin:2rem auto;font-family:system-ui, sans-serif;">
      <h1>📝 Bego-Pad</h1>
      <p>Base inicial creada. Astro en la raíz y API en <code>/api</code>.</p>
      <ul>
        <li>Firebase listo para inyectar claves de entorno.</li>
        <li>Scripts <code>pnpm dev</code> y <code>pnpm dev:api</code>.</li>
        <li>Deploy pensado para Vercel (dos proyectos).</li>
      </ul>
      <p>Prueba la API local en <code>http://localhost:3000/health</code> tras ejecutar <code>pnpm dev:api</code>.</p>
    </main>
  </body>
</html>
