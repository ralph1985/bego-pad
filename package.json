{
  "name": "bego-pad",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "dev:api": "pnpm --dir api dev",
    "build:api": "pnpm --dir api build",
    "start:api": "pnpm --dir api start",
    "typecheck": "tsc -p tsconfig.json && tsc -p api/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "astro": "^4.15.0",
    "typescript": "^5.6.2"
  }
}
